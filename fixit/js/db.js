/* FixIt – db.js stub (data now in PostgreSQL, accessed via API)
   This file exists so sw.js precache list resolves.
   All data ops are in api.js → Express → PostgreSQL. */
console.log('[FixIt] Data layer: PostgreSQL via REST API');
